
  <% unless @temp_images.blank? %>
   <%# unless @temp_image.blank? %>
      <% @temp_images.each do |ti| %>
        <%= image_tag ti.image_url(:main).to_s %>
        <%= link_to "remove", temp_image_path(ti), method: 'delete', remote: true %>
      <% end %>
    <%# end %>
  <% end %>
